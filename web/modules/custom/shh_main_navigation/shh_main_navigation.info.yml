name: 'SHH Main Navigation'
type: module
description: 'Renders the site header through the theme''s slotted hestehoj:navbar SDC (task 0032): site-name wordmark in the logo slot, the access-filtered main menu in the navigation slot (via the theme''s menu--main.html.twig), CTA slot deliberately empty — CTAs belong in page content per the 0032 client direction. Originally (task 0019) placed a plain system menu block instead, when the finding was that no navigation rendered anywhere platform-wide. Scoped to hestehoj only.'
core_version_requirement: ^10.2 || ^11
package: Custom
dependencies:
  - block:block
  - system:system
