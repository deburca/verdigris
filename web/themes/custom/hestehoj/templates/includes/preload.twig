{#
/**
 * @file
 * Preload the commonly used fonts for the headings and the body copy.
 *
 * Available variables:
 * - hestehoj_path: Returns the path to the Hestehoj theme.
 */
#}

<link rel="preload" href="{{ hestehoj_path }}/fonts/Outfit/latin.woff2" as="font" type="font/woff2" crossorigin />
